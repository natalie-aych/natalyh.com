import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#c2b280',
    },
    secondary: {
      main: '#8091c2',
    },
  },
});

export default theme;
